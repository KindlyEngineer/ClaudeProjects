# Baseline rules

- Never commit secrets. If you need a credential, expect it as an environment variable.
- Run the project's tests and linter before reporting a task complete.
- Prefer root-cause fixes over workarounds; if you apply a workaround, say so explicitly and explain why.
- Keep PR branches focused on a single task.
